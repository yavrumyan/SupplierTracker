### 🧠 Prompt for Replit Agent

> Please update the **Product Search** tab in my SupHub app according to the following points:
>
> ---
>
> ### ✅ General Notes:
>
> * **Do not change** the left-side menu — it is already fine.
> * All updates must happen **only inside the "Product Search" tab**.

---

### 🔧 Functional and Visual Edits:

1. **Remove the "Ready to Search" info box** — it is not needed anymore.

2. **Fix horizontal alignment** of content inside the Product Search tab.
   Align the left margin so that it matches the margins of other tabs/pages in the app for visual consistency.

---

### 🔍 Search Results Changes:

3. **Update columns in the search result table**.
   New column structure must be:

   ```
   Source | Supplier | Category | Brand | Model | Name | Price | Currency | Stock | MOQ | Notes
   ```

   * `"Source"` column (e.g., `price_list` or `offer`) stays first — keep it as-is.
   * `"Model"` = model or part number
   * `"Name"` = product name
   * `"MOQ"` = minimum order quantity (if not available, leave empty)
   * Do **not truncate** or cut long texts — allow line wrapping or overflow display.

---

### 🎯 Filters and Search Bar Update:

4. **Remove the following filters**:

   * `"Trading Categories"`
   * `"Trading Brands"`
   * `"Min. Reputation"`

5. **Add new dropdown filters** for:

   * `"Source"` (`price_list`, `offer`)
   * `"Supplier"` (based on list of suppliers in the system)
   * `"Category"` (from standardized price-lists)
   * `"Brand"` (from standardized price-lists)

6. **Add three keyword search input fields at the top** (instead of just one):

   * These are independent keyword inputs.
   * Search logic must be **AND / AND / AND** — a row is shown only if it matches all 3 keywords.
   * All 3 search fields must search across these 4 columns:

     * `"Category"`
     * `"Brand"`
     * `"Model"`
     * `"Name"`
   * Also search within saved offers if stored.

---

### 🚫 Deduplication Requirement:

7. **Ensure rows are not duplicated in results**.
   Example: if the same row matches multiple columns (e.g., “Ryzen” in both `"Category"` and `"Name"`), show it **only once**.

---

### 💾 Technical Notes:

* Keep the backend route efficient and compatible with SQLite or the current database setup.
* Maintain styling and responsiveness of the existing GUI.
* Keep pagination if already present, or use lazy loading if results are large.

---

Let me know if anything is unclear or if you need sample code snippets for search logic or deduplication.
